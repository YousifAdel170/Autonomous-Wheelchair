local_costmap:
  global_frame: odom
  robot_base_frame: chassis
  update_frequency: 5.0 
  publish_frequency: 9.0 
  static_map: false
  rolling_window: true
  width: 2.5
  height: 2.5
  resolution: 0.05
  transform_tolerance: 0.5
  cost_scaling_factor: 5
  inflation_radius: 0.15
  obstacle_range: 2.0  # Maximum range (in meters) at which obstacles are considered
